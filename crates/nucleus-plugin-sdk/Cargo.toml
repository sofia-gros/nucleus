[package]
name = "nucleus-plugin-sdk"
version = "1.0.0-alpha.1"
edition = "2021"
description = "Official Rust SDK for Nucleus IDE WASM Plugins"
authors = ["Nucleus Team"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
